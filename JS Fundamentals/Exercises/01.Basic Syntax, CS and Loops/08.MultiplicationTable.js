function multiplicationTable(n){

    for(let currentNumber = 1; currentNumber <= 10; currentNumber++){

        console.log(`${n} X ${currentNumber} = ${n * currentNumber}`)
    }
}
multiplicationTable(5);
multiplicationTable(2);